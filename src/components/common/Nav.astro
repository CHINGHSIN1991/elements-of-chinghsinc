---
import { Icon } from 'astro-icon/components';
import navData from '../../data/navdata';
import Link from './Link.astro';
import ThemeToggle from './ThemeToggle.astro';
---

<nav aria-label="Primary" class="w-full bg-background/80 backdrop-blur-sm shadow-sm dark:shadow-gray-800">
  <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center border-b-2 border-green-500">
    <a href="/" class="flex items-center gap-2 hover:opacity-80 transition-opacity border-2 border-red-500" aria-label="Go home">
      <Icon name="logomark" class="w-8 h-8" />
    </a>
    <div class="flex items-center gap-6 border-2 border-blue-500">
      <ul class="flex gap-6 items-center">
        {
          navData.map((item) => (
            <li>
              <Link
                text={item.name}
                href={item.path}
                style="secondary"
                isFilled={false}
                data-navLink
                icon={{
                  name: "lets-icons:alarmclock",
                  side: "left"
                }}
                classes="text-text hover:text-text-secondary transition-colors"
              />
            </li>
          ))
        }
      </ul>
      
    </div>
    <ThemeToggle />
  </div>
</nav>

<script>
  import "../ts/nav"
</script>